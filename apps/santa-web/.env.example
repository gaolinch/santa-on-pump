# Santa Web Frontend Environment Variables
# Copy this file to .env.local and update the values as needed

# Base URL for the Next.js application
# Used for server-side rendering absolute URLs
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Santa Block API URL (REQUIRED for production!)
# The backend API runs on port 3001 locally (see santa-block/docker-compose.yml)
# ⚠️ IMPORTANT: Set this to your production API URL in Vercel environment variables
NEXT_PUBLIC_API_URL=http://localhost:3001

# Stats API endpoint
# Used by lib/api.ts getStatsToday() function
NEXT_PUBLIC_STATS_API=http://localhost:3001/stats/today

# Proofs API endpoint
# Used by lib/api.ts getProof() function
NEXT_PUBLIC_PROOFS_API=http://localhost:3001/proofs/day

# Commitment root URL
# Used by lib/api.ts getCommitmentRoot() function
# Displays Merkle root and commitment hash on /commitments page
NEXT_PUBLIC_COMMITMENT_URL=http://localhost:3001/proofs/commitment

# Production URLs (set these in Vercel environment variables)
# NEXT_PUBLIC_BASE_URL=https://santa.yourdomain.com
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com
# NEXT_PUBLIC_STATS_API=https://api.yourdomain.com/stats/today
# NEXT_PUBLIC_PROOFS_API=https://api.yourdomain.com/proofs/day
# NEXT_PUBLIC_COMMITMENT_URL=https://api.yourdomain.com/proofs/commitment
